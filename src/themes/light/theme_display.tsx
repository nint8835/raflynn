import { BaseThemeDisplay } from '@/themes/common/theme_display';

export const ThemeDisplay = BaseThemeDisplay({
    containerClasses: 'bg-zinc-300 bg-opacity-70',
    titleClasses: 'text-zinc-800',
    subtitleClasses: 'text-zinc-700',
});
