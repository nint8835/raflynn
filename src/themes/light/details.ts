import type { ThemeDetails } from '@/themes/definition';

export const Details: ThemeDetails = {
    title: 'Light',
    subtitle: 'The default theme, but brighter',
    hint: 'Brighten things up a bit',
};
